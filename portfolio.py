class Portfolio:
	def_init_(self):
		self.positions = dict()
	
	def totalPrice(self)
		pass
	
	def sellBond(self,bond):
		pass
	
	def buyBond (self, bond, notional):
		if bond in self.positions:
			self.positions[bond] += notional
		else:
			newPosition = Position[notional,bond]
			self.position[newBond] = notional
